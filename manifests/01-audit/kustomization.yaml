apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - apiserver-audit.yaml
  - operators.yaml
  - lokistack.yaml
  - clusterlogforwarder-audit.yaml
# loki-s3-secret.example.yaml is intentionally NOT included: create the real secret from the vault.
