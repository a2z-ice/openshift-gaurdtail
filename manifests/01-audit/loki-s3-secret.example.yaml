# ---------------------------------------------------------------------------
# EXAMPLE ONLY - do not commit real credentials. Create from the vault / External Secrets Operator.
# The bucket must have versioning + Object Lock (compliance mode, 90 days) for tamper-evident retention.
#   oc create secret generic logging-loki-s3 -n openshift-logging \
#     --from-literal=access_key_id=... --from-literal=access_key_secret=... \
#     --from-literal=bucketnames=ocp-audit-loki --from-literal=endpoint=https://s3.eu-west-1.amazonaws.com --from-literal=region=eu-west-1
# ---------------------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: logging-loki-s3
  namespace: openshift-logging
  labels:
    guardrails.example.com/critical: "true"
type: Opaque
stringData:
  access_key_id: REPLACE_ME
  access_key_secret: REPLACE_ME
  bucketnames: ocp-audit-loki
  endpoint: https://s3.eu-west-1.amazonaws.com
  region: eu-west-1
